components:
  schemas:
    Book:
      type: object
      properties:
        name:
          type: string
          description: The title of the book.
          example: Nutuk
      required:
        - name
